name: holding_gesture_example
description: Demonstrates how to use the holding_gesture plugin.
homepage: https://github.com/gildaswise/holding_gesture

version: 1.0.0+10

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  holding_gesture:
    path: ../

flutter:
  uses-material-design: true